Colocá acá los libros fuente (`.epub`, `.pdf`).

No se commitean — están en `.gitignore`. El extractor los busca acá por defecto:

```bash
python3 scripts/extract-epub.py sapiens
python3 scripts/extract-epub.py --list
```
